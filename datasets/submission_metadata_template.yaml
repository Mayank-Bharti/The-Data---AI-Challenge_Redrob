# Redrob Hackathon — Submission Metadata Template
#
# Copy this file to your repo root as `submission_metadata.yaml` and fill it in.
# The fields here should match what you submit via the portal at upload time.
# Stage 3 review uses this file to verify your portal metadata.

# ============================================================================
# Team identity
# ============================================================================
team_name: "The Crimson Codex" # Used in leaderboard and announcements

primary_contact:
  name: "Mayank Bharti"
  email: "mayankbharti349@gmail.com" # Used for all organizer communication
  phone: "+91-9142673771" # Used for top-50 / top-10 outreach

team_members:
  - name: "Mayank Bharti"
    email: "mayankbharti349@gmail.com"
    role: "AI-ML Engineer" # Optional, e.g. "Team Lead", "Backend", "Data"
  - name: "Madhuri Kumari"
    email: "madhuri25042007@gmail.com"
    role: "Data Engineer"
  # Add more members as needed; solo participants list just one member

# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/Mayank-Bharti/The-Data---AI-Challenge_Redrob"
# Required. Must be reachable. Private repos OK if you can grant organizer
# access at Stage 3 (the email to add will be communicated then).

sandbox_link: "https://colab.research.google.com/drive/1QKydgpojM4Pj8BpdtNjeomtb9fllfypN?usp=sharing"
# Required. A working hosted environment where the ranker can be run on a small
# candidate sample. See Section 10.5 of submission_spec.md for acceptable
# platforms (HuggingFace Spaces, Streamlit Cloud, Replit, Colab, Docker, Binder).

reproduce_command: "python ranker.py"
# The single command that produces submission.csv from candidates.jsonl.
# Should run end-to-end within 5 minutes on CPU with 16GB RAM and no network.

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "Local Windows PC" # Or: "AWS EC2 c5.4xlarge", "Local Linux box", etc.
  cpu_cores: 8 # Number of CPU cores used
  ram_gb: 16 # Available RAM in GB
  python_version: "3.10.11" # python --version
  os: "Windows 11" # Or "Ubuntu 22.04 LTS", etc.
  uses_gpu_for_inference: false # Must be false — see compute constraints
  has_network_during_ranking: false # Must be false — no API calls during ranking
  pre_computation_required: true # true if you pre-compute embeddings or train models offline
  pre_computation_time_minutes: 20 # Approximate, if applicable

# ============================================================================
# AI tools declaration
# ============================================================================
# Transparency only — declared use is NOT penalized. Be honest. Stage 5 interview
# may verify these declarations against your code; declarations that contradict
# your code or your interview are flagged.
ai_tools_used:
  - "GitHub Copilot" # e.g. for autocomplete
  - "Gemini"
  # Other options: "ChatGPT", "Cursor", "Gemini", "Codeium", "Other", "None"

ai_usage_summary: |
  Briefly describe how AI tools were used.
  - "Used GitHub Copilot for autocomplete."
  - "Used Gemini to understand the problem and format the README.md. No candidate data was fed to any LLM APIs during runtime."

# ============================================================================
# Approach summary (optional but recommended)
# ============================================================================
methodology_summary: |
  The HDE-CM (Hybrid Dual-Encoder with Career Momentum) architecture strictly decouples heavy deep-learning from live inference to beat the 5-minute constraint. Offline, we parse 100,000 JSON profiles, run them through a custom Truth-Check Matrix to instantly drop chronological 'honeypot' anomalies, and generate dense mathematical vectors using a local HuggingFace Sentence-Transformer (all-MiniLM-L6-v2), caching the results to a .pkl database. Online, the ranker script loads the vector cache into RAM and performs highly optimized NumPy Cosine Similarity calculations against the embedded Job Description. The base semantic score is then multiplied by Career Momentum heuristics (e.g., boosting active GitHub coders, exponentially decaying inactive profiles). Finally, it dynamically outputs the Top 100 leaderboard with human-readable XAI reasoning sentences. Runtime is 5.7 seconds for 43k+ viable candidates on a local CPU.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true # I have read submission_spec.md in full
  code_is_original_work: true # My code is my team's original work (using AI as a tool is fine)
  no_collusion: true # I have not coordinated my submission with other teams
  honeypot_check_done: true # OPTIONAL — set true if you explicitly checked for honeypots in your ranking
  reproduction_tested: true # I have tested that my reproduce_command runs end-to-end
